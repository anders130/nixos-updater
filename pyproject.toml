[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
dependencies = []
name = "nixos-updater"
requires-python = ">=3.11"
version = "0.1.0"

[project.scripts]
nixos-updater = "nixos_updater.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/nixos_updater"]
